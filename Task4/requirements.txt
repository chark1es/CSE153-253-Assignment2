# Task 4 — Cross-Instrument Note Timbre Transfer
# Python 3.9+ (developed on 3.9.6). Install with:  pip install -r requirements.txt
torch>=2.0
torchaudio>=2.0
librosa>=0.10
soundfile>=0.12      # writes MP3 directly (libsndfile >= 1.1), no ffmpeg needed
numpy
pandas
matplotlib
tqdm
jupyter
ipython
